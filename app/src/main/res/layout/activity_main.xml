<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="@color/darkgreybackground">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true">

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/want_to_send"
            android:id="@+id/sendButton"
            android:layout_gravity="center_horizontal"
            android:elegantTextHeight="false"
            android:background="#b5fa88"
            android:layout_margin="2dp"
            android:onClick="sendButtonOnClick" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/want_to_get"
            android:id="@+id/getButton"
            android:layout_gravity="center_horizontal"
            android:background="#b5fa88"
            android:layout_margin="2dp"
            android:onClick="getButtonOnClick"
            android:nestedScrollingEnabled="false" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Automatic Mode"
            android:id="@+id/autoModeButton"
            android:layout_gravity="center_horizontal"
            android:background="#b5fa88"
            android:layout_margin="2dp"
            android:onClick="autoModeButtonOnClick"
            android:nestedScrollingEnabled="false" />
    </LinearLayout>
</RelativeLayout>
